{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "cmake .. -G \"MinGW Makefiles\" && mingw32-make && HotelApplication.exe",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "options": {
                "cwd": "build"
            },
            "problemMatcher": []
        }
    ]
}